import { StyleSheet } from "react-native";
import { BG_COLORS } from "../../../../core/theme/colors";

export const styleLoginScreen = StyleSheet.create({
  container: {
    flex: 1,
    gap: 20,
    backgroundColor: BG_COLORS.bgPrimary,
    paddingHorizontal: 20,
  },
});
